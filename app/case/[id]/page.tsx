import Link from 'next/link';
import { demoCase } from '@/lib/demo-data';

export default function CasePage(){const c=demoCase;const v=c.verdict!;return <main className="shell"><nav className="nav"><Link href="/" className="brand"><span className="mark">V</span>VERDICTX</Link><div className="navlinks"><Link href="/dashboard">Dashboard</Link><Link href="/cases">Cases</Link><Link href="/agents">Agents</Link></div><span className="mono navtag">{c.id}</span></nav><div className="dashboard"><div className="pagehead"><div><div className="eyebrow">Case file / {c.id}</div><h1>{c.title}</h1><p className="muted">{c.plaintiff} · claimant &nbsp; / &nbsp; {c.defendant} · respondent</p></div><span className="status">{c.status}</span></div><div className="casegrid"><section className="panel"><div className="panelhead"><h2>CASE FLOW</h2><span className="mono muted">AUDIT TRAIL</span></div><div className="timeline">{['Agreement created','Escrow funded — 500 USDC','Delivery submitted — 20 items','Dispute opened — 3 items challenged','Evidence collected — 4 artifacts','Investigation — external sources analyzed','Adjudication — Intelligent Contract','Consensus — result validated','Verdict finalized — 85% fulfillment','Settlement — 425 / 75 USDC'].map((s,i)=><div className="event" key={s}><div className="dot"/><div style={{flex:1}}><strong>{s}</strong>{i===5&&<p>Web evidence independently retrieved and assessed against the acceptance criteria.</p>}</div><span className="mono muted">{i<9?'DONE':'SETTLED'}</span></div>)}</div></section><aside className="panel"><div className="panelhead"><h2>VERDICT</h2><span className="status">FINAL</span></div><div className="side"><div className="eyebrow">{v.decision.replaceAll('_',' ')}</div><div className="big">{v.fulfillmentScore}%</div><div className="bar"><i style={{width:`${v.fulfillmentScore}%`}}/></div><div className="kv"><span>Valid</span><strong>{v.validDeliverables}</strong></div><div className="kv"><span>Invalid</span><strong>{v.invalidDeliverables}</strong></div><div className="kv"><span>Confidence</span><strong>{v.confidence}%</strong></div><div className="verdict"><div className="eyebrow">Settlement</div><div className="kv"><span>Provider</span><strong>$425</strong></div><div className="kv"><span>Buyer refund</span><strong>$75</strong></div></div></div></aside></div><div className="detailgrid"><section className="panel"><div className="panelhead"><h2>EVIDENCE</h2><span className="mono muted">{c.evidence.length} ITEMS</span></div>{c.evidence.map(e=><div className="evidence" key={e.id}><div><span className="tag">{e.type}</span><strong>{e.title}</strong><p>{e.description}</p></div><span className="status">{e.status}</span></div>)}</section><section className="panel"><div className="panelhead"><h2>ADJUDICATOR REASONING</h2><span className="mono muted">GENLAYER</span></div><div className="reasoning"><p>{v.reasoning}</p>{v.findings.map((f,i)=><div className="finding" key={f}><span>0{i+1}</span>{f}</div>)}</div></section></div></div></main>}
